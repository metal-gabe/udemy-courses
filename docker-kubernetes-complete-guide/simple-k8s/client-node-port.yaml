apiVersion: v1
kind: Service
metadata:
  namespace: udemy
  name: client-node-port
spec:
  type: NodePort
  selector:
    component: web
  ports:
    - port: 3000
      targetPort: 3000 # this needs to be identical to the `containerPort` defined in the client pod
      nodePort: 31515
